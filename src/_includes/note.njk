{% extends "index.njk" %}

{% block content %}
  <span class="journal-entry--date">{{ page.date | formatDate }}</span>
  {%- if codepen -%}
    <a class="journal-entry--codepen-link" href="{{ codepen }}">
      on codepen
    </a>
  {%- endif -%}
  {%- if image -%}
  <img src="{{ image }}" alt="{{ title | striptags | safe }}" title="{{ title | striptags | safe }}">
  {%- endif -%}
  <h1>{{ title | markdown | safe }}</h1>
  {{ content | safe }}
{% endblock %}